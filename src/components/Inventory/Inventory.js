import React from 'react';

const Inventory = () => {
    return (
        <div style={{
            display: 'flex',
            justifyContent:' center',
            alignItems: 'center',
            marginTop: '100px'
        }}>
            <h2>This is Inventory</h2>
        </div>
    );
};

export default Inventory;